server:
  port: 0

spring:
  application:
    name: user-service

# DB ??
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/msabackendex
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    show-sql: true

eureka:
  instance:
    #? ?????? ???? ???? ??
    instance-id: ${spring.client.cloud.hostname}:${spring.application.instance_id:${random.value}}
  client:
    register-with-eureka: true # ??? ??? ??
    fetch-registry: true # ??? ???? ?? ?? ??? ??
    service-url:
      defaultZone: http://127.0.0.1:8761/eureka # ??? ??? ?? / discovery-server